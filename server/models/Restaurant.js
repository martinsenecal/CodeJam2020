const mongoose = require('mongoose');

/*
Informations that we can used for each restaurant:

Places Details: Only API that we need -> will return all of this

types[] take only food...
name
photos (up to 10 photos)
place_id (unique id)
vicinity
opening_hours -> open_now
website
price_level (0 to 4)
rating
review ? up to 5 reviews
user_ratings total
url
vicinity for address
*/
