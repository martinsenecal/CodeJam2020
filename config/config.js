require('dotenv').config();

const {​​ PORT, MONGODB_URL, GOOGLE_API_KEY, HOME_PAGE_DOMAIN}​​ = process.env;

const config = {​​ PORT, MONGO_URL, GOOGLE_API_KEY, HOME_PAGE_DOMAIN}

module.exports = config;​​
